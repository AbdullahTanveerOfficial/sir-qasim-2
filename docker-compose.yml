services:
  web:
    build: .
    ports:
      - "5000:5000"
    restart: always   # ✅ ADD THIS
    depends_on:
      - db

  db:
    image: mongo
    volumes:
      - db_data:/data/db
    restart: always   # ✅ ADD THIS