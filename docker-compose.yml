version: "3.9"  # always good to specify version

services:
  web:
    build: .
    ports:
      - "5000:5000"
    restart: always
    depends_on:
      - db

  db:
    image: mongo
    volumes:
      - db_data:/data/db
    restart: always

volumes:
  db_data: